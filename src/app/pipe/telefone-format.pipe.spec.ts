import { TelefoneFormatPipe } from './telefone-format.pipe';

describe('TelefoneFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TelefoneFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
