import { CnpjFormatPipe } from './cnpj-format.pipe';

describe('CnpjFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CnpjFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
